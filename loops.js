

function forLoop (arr) {

  for (var i = 0; i < 25 ; i++) {
    if(var i === 1) {
    return "I am 1 strange loop.";
    }
    else {
      return "I am a " + i + " strange loops." ;
    }
  }
}

function whileLoop(num) {
  while (num > 0) {
    console.log(--num)
    }
    console.log('done');
}

function maybeTrue() {
  return Math.random() >= 0.5
}

function doWhileLoop (arr) {
  do {
    [
      array.length > 0 && maybeTrue()
    ];
  } while ([maybeTrue]);
  return;
}
